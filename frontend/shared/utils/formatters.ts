export const formatDateShort = (iso: string, locale?: string) => { if (!iso) return ''; const date = new Date(iso); return new Intl.DateTimeFormat(locale || 'en-US', { month: 'short', day: 'numeric', year: 'numeric' }).format(date); }; export const formatRelativeTime = (iso: string, locale?: string) => '';
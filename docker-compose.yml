version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:3000" # Standard HTTP
      - "443:3000" # Standard HTTPS (requires SSL setup later)
    environment:
      - NODE_ENV=production
    restart: always
